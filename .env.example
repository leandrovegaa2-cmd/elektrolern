# Geräte-Sync (optional). Ohne diese Werte läuft die App normal, nur lokal.
# Werte aus dem Supabase-Projekt: Settings -> API.
# Lokal in .env.local kopieren (nicht committen); bei Netlify als Build-Env setzen.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
